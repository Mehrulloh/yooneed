= modal title: "Menge auswählen" do
  = form_with model: @order, data: {turbo_frame: "_top"} do |f|
    .field.mb-2.flex.flex-col.text-black
      = f.hidden_field :product_id, value: @product.id
      = f.hidden_field :name, value: @product.name
      = f.label :amount, class: "text-black mb-2"
      = f.number_field :amount, class: "text-black mb-2"
    .field.flex.justify-center.mt-10.gap-2
      = f.submit 'Save', class: 'product-btn bg-red-600 mt-2 w-48 cursor-pointer hover:bg-red-700'
      = link_to 'Back', dashboard_path, class: "product-btn bg-red-600 mt-2 w-48 cursor-pointer hover:bg-red-700 text-center"