.board.flex-col.mb-4
  %h2.board-titel Users
  - @users.each do |user|
    .board-content.mb-2
      .flex.justify-between.items-center
        .flex.justify-between.items-center.gap-2
          %p="#{user.id})"
          %p=user.full_name
        .flex.justify-between.items-center.gap-2
          = link_to "Show", supervisor_user_path(user), class: "bg-blue-800 text-white px-4 py-2 rounded cursor-pointer my-2"
          = link_to "Edit", edit_supervisor_user_path(user), class: "bg-teal-800 text-white px-4 py-2 rounded cursor-pointer my-2"
          = link_to 'Destroy', supervisor_user_path(user), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "bg-red-800 py-2 px-2 text-white rounded hover:bg-red-700"